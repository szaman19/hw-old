Searching algorithm:

I would first sort the unsorted array. Then you parse through the list checking if each element matches the desired the string. If you find it return the index if not you will return an arbitrary number such as -1. 