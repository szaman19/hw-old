import java.io.*;
import java.util.*:

public class quickSort{
	public static int[] partition(int[] sArray, int lower, int higher, int[] sdArray){
		Random ranGenerator = new Random();
		int pivot = ranGenerator.nextInt(higher); 

		if (sArray.length == 1 || sArray.length == 2){
			
		}
	}
}