public class Search{
    /*public int rbsearch(int n, int[] L){
	int mid = (L.length() - 1)/2;
	if(L.length == 1 && n != L[0]){
	    return -1;
	}else if(L[mid] == n){
	    return mid;
	}else if(L[mid] > n){
	    int[] new = new int[L.length - mid];
	    for(int i = 0; i < L.length; i++){
		new[i] = L.length
	    return rbsearch(n, 
    */
    //not exactly sure how to break the array into a half excluding the middle value
}
	
