/*import java.util.*;

public class QuickSort{
    public void QuickSort(int[] a, int low, int high){
	if(a.length <= 1){
	    return a;
	}
	int i = (int)(Math.random() * (low - high)) + low;
	int pivot = a[i];
	ArrayList<String> left;
	ArrayList<String> right;
	for(int j = 0; j < a.length; j++){
	    if(pivot > a[j]){
		left.add(a[j]);
	    }
	    if(pivot < a[j]){
		right.add(a[j]);
	    }
	}
    }
}*/
	
	