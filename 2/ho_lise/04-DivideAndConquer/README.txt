Searching more efficiently:
1. split the array in two
2. compare array to one of the middle elements
   if greater, repeat for the array with the larger elements
   if less, repeat for array with the lesser elements
   if equal, end and return index
3. if this continues til a null array, return -1;
